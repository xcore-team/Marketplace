# xdevkeys — variables d'environnement
# Copier en .env et remplir les valeurs

# Clé maître pour chiffrer les signing keys des développeurs
# Générer : python -c "import secrets; print(secrets.token_hex(32))"
DEVKEYS_MASTER_KEY=changeme-generate-a-64-char-hex-key
