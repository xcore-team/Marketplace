# ── Production — api.xcorehub.dev ─────────────────────────────────────────────
# Variables intégrées au build (NEXT_PUBLIC_*).
# Pour déployer : npm run build avec NODE_ENV=production (ce fichier est lu automatiquement).
# Ne jamais commiter les secrets ici.

NEXT_PUBLIC_API_URL=https://api.xcorehub.dev
NEXT_PUBLIC_MOCK_MODE=false
